const HOST = window.location.protocol + "//" + window.location.host + SYSNAME;

// controllers
const PATH_TO_CONTROLLER = 'data/controller';
const LOGIN_CONTROLLER = HOST + '/' + PATH_TO_CONTROLLER + '/LoginController.php';
const USER_CONTROLLER = HOST + '/' + PATH_TO_CONTROLLER + '/UserController.php';
const EMPLOYEE_CONTROLLER = HOST + '/' + PATH_TO_CONTROLLER + '/EmployeeController.php';
const CLOCKING_CONTROLLER = HOST + '/' + PATH_TO_CONTROLLER + '/ClockingController.php';
const ATTENDANCE_CONTROLLER = HOST + '/' + PATH_TO_CONTROLLER + '/AttendanceController.php';
